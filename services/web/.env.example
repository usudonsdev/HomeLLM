# Copy to .env.local (gitignored) for desktop `npm run dev`.
# Browser talks to Windows APIs over Tailscale — Pi only serves static files.
NEXT_PUBLIC_JOB_HUNTING_API_URL=http://127.0.0.1:8000
NEXT_PUBLIC_VIDEO_INGEST_API_URL=http://127.0.0.1:8090
