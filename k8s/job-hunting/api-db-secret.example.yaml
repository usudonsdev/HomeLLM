apiVersion: v1
kind: Secret
metadata:
  name: api-db-secret
  namespace: job-hunting
type: Opaque
stringData:
  DATABASE_URL: "postgresql+psycopg://<POSTGRES_USER>:<POSTGRES_PASSWORD>@postgres:5432/homellm"
# Local only — do not commit filled values.
